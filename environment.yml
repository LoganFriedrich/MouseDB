# Conda environment for Connectome Data Entry
#
# Create with:
#   conda env create -f environment.yml -p "Y:/LAB_ROOT/envs/MouseDB"
#
# Activate with:
#   conda activate "Y:/LAB_ROOT/envs/MouseDB"

name: MouseDB
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pandas>=1.5
  - openpyxl>=3.0
  - pyarrow>=10.0
  - sqlalchemy>=2.0
  - pyqt>=5.15
  - pip:
    - -e "Y:/LAB_ROOT/Unified_Data/unified_data/MouseDB"
